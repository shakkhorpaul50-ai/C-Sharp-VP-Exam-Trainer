# C# Visual Programming Exam Trainer

Last-day exam preparation — 9 Topics • 27 Problems • Code Checker

Live: `index.html` (also `C#.html` and `C-Sharp.html` for compatibility)

**Deploy to Cloudflare Pages:**
- Framework preset: **None**
- Build command: *(empty)*
- Build output directory: `/` (root)
- Root file is `index.html` — Cloudflare serves `/` as `index.html`. The original `C#.html` contains `#` which breaks URLs (`#` is a fragment), so `index.html` is the Cloudflare-safe entry point.

Developed by [Shakkhor Paul](https://www.github.com/shakkhorpaul50-ai)
